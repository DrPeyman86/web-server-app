/*"start" is specific keyword so when horoku hosts the app, it doesn't know what the name of
    //the file is that it needs to start the app. But it does know that it should look for a script named "start" to run in 
    //command line. <npm start>. */